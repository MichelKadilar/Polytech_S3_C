#include <stdio.h>
#include <stdlib.h>


int iterative_iterate(int (*fct)(int, int), int tab[], int tabSize){
    int *tmp = *tab;
    for(int i=0; i<tabSize; i++, tab++){
        *tmp = 
    }
}

int iterate(int (*fct)(int, int), int tab[], int tabSize){
    if(tabSize==1){
        return tab[0];
    }
    else {
        return iterate()
    }
}

int main(void){

}